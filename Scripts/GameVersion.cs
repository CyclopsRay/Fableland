/// <summary>
/// Project version. Convention: starts at 0.1.0 and the patch number is bumped
/// by +0.0.1 on every commit unless told otherwise. Shown top-left on screen.
/// Keep this in sync with the repo-root VERSION file.
/// </summary>
public static class GameVersion
{
    public const string Current = "0.2.0";
}
